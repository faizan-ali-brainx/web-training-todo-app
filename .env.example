# Set to "false" once the real backend (Day 5, NestJS) is ready to use instead of the mock API
VITE_USE_MOCK_API=true

# Base URL of the real backend API (only used once VITE_USE_MOCK_API=false)
VITE_API_BASE_URL=http://localhost:3000/api
